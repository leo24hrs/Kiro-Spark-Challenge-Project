import type { GladiatorResult } from '../types.js';
/**
 * Renders the Game Over ASCII-art markdown scoreboard.
 * Pure function — no side effects, no async, no fix suggestions.
 */
export declare function renderScoreboard(results: GladiatorResult[], command: string, developerId: string, branchId: string): string;
//# sourceMappingURL=renderer.d.ts.map