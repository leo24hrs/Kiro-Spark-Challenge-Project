Dev executes risky command
        ↓
Shadow clone spins up (Neon branch)
        ↓
Command executes on clone
        ↓
  ┌─────────────────────────────┐
  │     THE COLOSSEUM OPENS     │
  │                             │
  │  Gladiator 1: SQL Injector  │
  │  Gladiator 2: Concurrency   │
  │  Gladiator 3: Cascade Kill  │
  │  Gladiator 4: Load Breaker  │
  │  Gladiator 5: Rollback Test │
  └─────────────────────────────┘
        ↓
  Each gladiator is a sub-agent
  with its own tool loop, running
  in parallel against the clone
        ↓
  Results aggregated into
  a unified threat report
        ↓
  Game Over UI renders the
  full arena scoreboard